# A fine-grained GitHub token with read-only metadata access to public repositories.
FORGEPULSE_GITHUB_TOKEN=
FORGEPULSE_FILTER=carlok/*
FORGEPULSE_SYNC_INTERVAL=1h

# Set to false after supplying a token. Demo data is useful for first-run UI work.
FORGEPULSE_DEMO=true
